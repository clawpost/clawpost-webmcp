{
  "name": "@clawpost/webmcp",
  "version": "0.1.0",
  "description": "In-page WebMCP tools for ClawPost — register site tools that a browser agent can call while the human watches.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/clawpost/clawpost-webmcp.git"
  },
  "exports": {
    ".": "./src/index.ts",
    "./react": "./src/react.ts",
    "./mock": "./src/mock.ts"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "peerDependenciesMeta": {
    "react": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
